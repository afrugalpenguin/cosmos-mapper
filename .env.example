# Cosmos DB Connection
# Choose ONE authentication method:

# Option 1: Key-based authentication
COSMOS_ENDPOINT=https://your-account.documents.azure.com:443/
COSMOS_KEY=your-primary-or-secondary-key

# Option 2: Azure AD / Managed Identity authentication
# Set COSMOS_ENDPOINT above, leave COSMOS_KEY empty or remove it
# The tool will use DefaultAzureCredential (works with managed identity,
# Azure CLI, VS Code, environment variables, etc.)

# Database Configuration
# Leave empty to document ALL databases in the account
# Or specify a comma-separated list to filter:
# DATABASES=db1,db2,db3
DATABASES=

# Optional: Sample size per container (default: 100)
SAMPLE_SIZE=100
